import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Daily Entries",
  description: "Log and manage daily harvest entries — record bunches, tonnage, worker counts, and backlogs.",
  path: "/daily-entries",
});
