import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Dashboard",
  description: "Overview of your palm plantation productivity — key metrics, recent activity, and performance at a glance.",
  path: "/dashboard",
});
