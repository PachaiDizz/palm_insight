import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Reports",
  description: "View monthly reports, production analytics, and export harvesting data as Excel spreadsheets.",
  path: "/reports",
});
