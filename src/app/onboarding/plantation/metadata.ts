import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Setup Plantation",
  description: "Set up your first palm oil plantation details — blocks, rancangan, and peringkat information.",
  path: "/onboarding/plantation",
});
