import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Setup Team",
  description: "Add your team leaders during onboarding to get started with PalmInsight.",
  path: "/onboarding/teams",
});
