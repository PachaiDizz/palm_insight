import { pageMetadata } from "@/lib/site";

export const metadata = pageMetadata({
  title: "Login",
  description: "Sign in to your PalmInsight account to manage your palm oil plantations.",
  path: "/login",
});
